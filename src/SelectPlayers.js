import React, {useState, useEffect } from 'react';


export default function SelectPlayers({first_name, last_name}){

    return(
        <>
    <div>{first_name}</div>
    <div>{last_name}</div>

        </>
    )
}